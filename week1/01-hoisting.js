const browserVersion = "Chrome"

function getBrowserVersion() {

    if(browserVersion.startsWith("Chrome")){

        let browserVersion = "Firfox"
       console.log(browserVersion)
    }

    console.log(browserVersion)
}

getBrowserVersion();

